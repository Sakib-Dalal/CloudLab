[package]
name = "cloudlab-desktop"
version.workspace = true
edition.workspace = true
license.workspace = true

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
cloudlab = { path = "../crates/cloudlab" }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
