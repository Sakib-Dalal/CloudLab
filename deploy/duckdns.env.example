# Copy to .cloudlab/duckdns.env and restrict that file to your user.
# Use only the name you registered, without .duckdns.org, lab., or *.
DUCKDNS_SUBDOMAIN=my-cloudlab
DUCKDNS_API_TOKEN=REPLACE_WITH_YOUR_DUCKDNS_TOKEN
